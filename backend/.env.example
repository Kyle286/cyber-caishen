# DeepSeek 配置（OpenAI 兼容接口）
# 不填或调用失败时，Agent 会自动回退到本地规则模板，保证演示可用
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 数据库文件路径
DB_PATH=caishen.db

# 请求 LLM 的超时时间（秒）
LLM_TIMEOUT=20
